#download on your local machine all remote changes
git fetch

# merge all remote changes with your local changes
git merge

#download remote changes and merge with your local changes
(git fetch + git merge) = git pull