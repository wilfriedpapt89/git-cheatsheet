# When we have created a repo in our local environnement 
#with git init and we want to sync with remote repo


git remote add origin https://github.com/nom_du_repo.git
git branch -M branch_name
git push -u origin branch_name